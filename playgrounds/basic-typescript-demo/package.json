{
  "name": "basic-typescript-demo",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "rimraf dist && rollup -c rollup.config.js -w",
    "build": "rimraf dist && rollup -c rollup.config.js"
  },
  "devDependencies": {
    "rollup-plugin-ast-explorer": "workspace:*"
  }
}
